<!--pages/home/home.wxml-->
<view>Hello World!</view>
<view>{{name}}</view>
<view>{{age}}</view>
<view wx:for="{{student}}" wx:key="key">{{item.name}}</view>
<view>当前计数为：{{counter}}</view>
<button size="mini" bindtap="addClick" >+</button>
<button size="mini" bindtap="subClick">-</button>